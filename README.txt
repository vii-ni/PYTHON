BANCO EM SQLITE3 COM PYTHON-

    Este é um código python que utiliza a biblioteca sqlite3.
    Utilizando o sqlite3 no python temos a possibilidade de criar um banco de dados local, isso apenas usando o comando connect("<nome_do_banco.db>"), assim seu banco já será criado.

create by : @v.ii.n.i
